# AURA v1.0.7 — korrupte Browser-Queue unverändert bewahren

Workspace: `/home/ivo/projects/AURA_Quant_Terminal`

## Rolle und Regeln

Bestehenden uncommitteten v1.0.7-Stand behalten. Striktes TDD. Keine Commits, Tags, Pushes, Releases oder Deployments. Nur `Symbiose_Dashboard.html` und `tests/test_cross_device_sync.js` ändern.

## Belegter Datenverlust

`SyncEngine.restoreQueue()` erkennt korruptes JSON zwar und bleibt offline, löscht aber danach den ursprünglichen localStorage-Wert:

- `Symbiose_Dashboard.html:6263-6267`
- `localStorage.removeItem(SYNC_QUEUE_KEY)`

Reproduktion:

```json
{"queueCorrupt":true,"status":"offline","pending":0,"queueStillPresent":false,"posts":0}
```

Damit wird möglicherweise noch manuell oder zukünftig rekonstruierbare Nutzerintention vernichtet. Korrektes fail-closed Verhalten:

- rohe Queue-Bytes unverändert erhalten
- `queueCorrupt = true`
- `pending = []`
- Status `offline` / `nicht synchronisiert`
- keine POSTs
- keine automatische Verarbeitung bis sichere Recovery

## TDD — RED vor GREEN

1. Ändere zuerst den bestehenden Test `durable queue: corrupt JSON is ignored without sending` in `tests/test_cross_device_sync.js` oder ergänze einen präzisen separaten Test.
2. Speichere den Rohwert `'{broken'`.
3. Rufe `restoreQueue()`/`init()` auf.
4. Fordere zusätzlich:
   - `shared['aura-sync-pending-v1'] === raw`
   - Key bleibt vorhanden
   - `queueCorrupt === true`
   - `pending.length === 0`
   - keine POSTs
   - Status bleibt `offline`
5. Führe den gezielten Node-Test vor Produktionscode aus. Erwartetes RED: Queue-Key fehlt/Rohwert wurde gelöscht.
6. Minimaler Produktionsfix: korrupten Rohwert nicht entfernen oder überschreiben. Sonstige Queue-Pfade unverändert lassen.
7. Gezielter Test GREEN, dann Volltests.

## Verifikation

- `node tests/test_cross_device_sync.js` — erwartete neue Gesamtzahl, 0 Fehler
- `python3 -m pytest -q`
- `node tests/test_engine_full.js`
- `python3 tests/pine_static_check.py`
- `python3 scripts/release_check.py` → Exit 0, exakt `VERDICT: GO`
- `python3 scripts/build_package.py`
- ZIP-Safety und Hashgleichheit der Kernartefakte
- `git diff --check`
- Python-Compile und relevante Shell-Syntax

## Ergebnisformat

- `summary`
- `files_changed`
- `red_tests`
- `green_tests`
- `package_verification`
- `unverified`
