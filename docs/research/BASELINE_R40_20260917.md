# AURA R40 — Baseline vor Neuentwicklung

**Datum:** 2026-09-17 (Europe/Berlin)
**Zweck:** Eingefrorener Ausgangszustand vor der evidenzbasierten Neuentwicklung (Mandat „AURA — evidenzbasierte Neuentwicklung"). Alle späteren Fehler müssen diesem Stand oder einer Audit-Änderung zugeordnet werden können.

## 1. Ausgecheckter Stand

| Merkmal | Wert | Beleg |
|---|---|---|
| Commit (HEAD) | `de25830de9884a4a6b9019d1ecf0bf2da6f147a9` | `git rev-parse HEAD` |
| Commit-Datum | 2026-09-16 21:41:10 +0200 | `git log -1` |
| Commit-Subject | Merge pull request #54 from trixr1907/docs/r39-final-release-evidence | `git log -1` |
| Basis-Branch | `main` (= `origin/main`, identischer SHA via `git ls-remote`) | `git ls-remote` |
| Arbeits-Branch | `audit/r40-baseline` | `git checkout -b` |
| Produktversion | 2.5.0 | `VERSION` |
| Working Tree | sauber (frischer Clone nach `/home/ivo/projects/AURA_v2`) | `git status` |

## 2. Runtime- und Tool-Versionen (lokale Audit-Umgebung, WSL)

| Tool | Version |
|---|---|
| Python | 3.12.3 |
| Node.js | v26.5.1 |
| npm | 12.0.2 |
| Docker | 29.1.3 (build 29.1.3-0ubuntu3~24.04.2) |
| docker compose (Plugin) | **nicht installiert** (`docker: unknown command: docker compose`) — lokale Compose-Validierung eingeschränkt; Ziel-VM benötigt das Plugin |
| git | 2.43.0 |
| pip | 24.0 |
| requirements.txt | playwright==1.62.0, pytest==9.1.1 |

## 3. Test-Baseline (Befehle, Ergebnisse, Exit-Codes)

| Suite | Befehl | Ergebnis | Exit |
|---|---|---|---|
| Pytest (gesamt) | `python3 -m pytest -q` | **508 passed, 69 subtests passed in 13.74s** | 0 |
| JS Discovery (alle `tests/test_*.js`, einzeln via `node`) | Loop über 98 Dateien | **98 PASS / 0 FAIL** | 0 |
| Release-Gate | `python3 scripts/release_check.py` (in disposable Worktree `/tmp/aura_gate`) | **VERDICT: SOFTWARE_GO / MODEL_NO_EVIDENCE (real) · synthetic-gate: PAPER_CANDIDATE · lockbox-eval: UNUSED** | 0 |
| Ledger-Verifikation | `python3 scripts/verify_ledger.py` | `ok:true, last_entry_id EXP-032, entry_count 7, chain_head ac613227…116b, total_model_experiments 10` | 0 |
| Referenz-Backtest (Python-Oracle) | `python3 tests/reference_backtest.py` | ALL ASSERTIONS PASSED (accounting, reconcile, 4 Folds, DSR N=45, PAVA-Kalibrierung) | 0 |
| Pine Static Check | `python3 tests/pine_static_check.py` | OK (855 Zeilen, 4 request.security, 5 GM plots) | 0 |
| Pine↔JS Golden | `node tests/compare_pine_js_golden.js` ohne Args | Usage-Hinweis, Exit 2 — wird parametrisiert **innerhalb des Release-Gates** ausgeführt (dort OK, 5 Fixtures) | 2 (erwartungsgemäß, kein Defekt) |

Raw-Logs: `/tmp/aura_baseline/{pytest,js_tests,release_check,verify_ledger,reference_backtest,pine_golden,pine_static}.log` (flüchtig; Kernzahlen oben festgehalten).

**Übereinstimmung mit RELEASE_v2.5.0.md:** Die dort behaupteten Zahlen (pytest 508 + 69 subtests, Gate-Verdict, Ledger-Head) reproduzieren exakt.

## 4. Befunde bei der Bestandsaufnahme (vor Subaudits)

| # | Befund | Schwere | Beleg |
|---|---|---|---|
| B-01 | **/api/bot-config ohne serverseitige Validierung und ohne Token-Auth im released Code.** Die Funktionen `validate_bot_config` / `check_relay_token` existieren in `bitget_relay.py@de25830` nicht (`grep` → kein Match). Ein untracked WIP-Test `tests/test_bot_config_security.py` im Alt-Checkout (`/home/ivo/projects/AURA_Quant_Terminal`, Branch `fix/r39-config-validation-and-auth`) prüft genau diese Funktionen red-first. Nutzeränderung wird respektiert und als Anforderung übernommen. | HIGH | grep + WIP-Test |
| B-02 | Browser-Engine und Headless-Runner enthalten beide Quant-Logik (historisch gewachsen); Parität ist über Tests abgesichert, aber es gibt faktisch zwei Produktionspfade derselben Formeln. Detaillierung im Dataflow-Subaudit. | HIGH (Architektur) | docs/architecture.md §1, §8.1 |
| B-03 | Persistenz = JSON-Dateien (`aura_shared_state.json`, `aura_signal_center_state.json`, `shadow_log.jsonl`) ohne echte DB; Migration v1→v2 vorhanden, aber kein transaktionales Mehrprozess-Modell. | MEDIUM | docs/architecture.md §8 |
| B-04 | Lokale Audit-Umgebung ohne `docker compose`-Plugin → Compose-Smoke-Test lokal nicht ausführbar; wird auf Ziel-VM verlegt (Deployment-Status vorerst LOCAL_ONLY/NOT_RUN). | INFO | `docker compose version` |
| B-05 | Dokumentations-Claims CLM-16..18 in `claims.csv` als FALSCH markiert (veraltete Hashes/Testzahlen) — Dokudrift bestätigt, niedrige Schwere. | LOW | claims.csv |

## 5. Mandats-Antworten (Stakeholder, 2026-09-17)

1. **Ziel-VM:** Ubuntu/Debian-VM auf Proxmox existiert bereits, ausreichende Ressourcen.
2. **Bestandsinstallation:** Ja, AURA läuft bereits (Docker). **Laufzeit-State muss migriert werden** (Paper-Trades, Historie, shadow_log, Ledger).
3. **Zugriff:** LAN + WireGuard/Tailscale, **kein offener Router-Port**.
4. **Backup-Ziel:** Noch keines vorhanden → Vorschlagspflicht (z. B. PBS/NAS; Restic/Borg auf SMB/NFS-Share).
5. **Benachrichtigungen:** ntfy beibehalten.

**Dokumentierte Entwicklungsannahme (nicht gefragt, konservativ):** Single-User-System (ein Betreiber, PC + Handy). Autorisierung wird dennoch serverseitig erzwungen (VPN ersetzt keine Autorisierung).

## 6. Respektierte Nutzeränderungen

- Alt-Checkout `/home/ivo/projects/AURA_Quant_Terminal` (Branch `fix/r39-config-validation-and-auth`, untracked `tests/test_bot_config_security.py`) wird **nicht** verändert. Die darin kodierten Anforderungen (Config-Validierung, Relay-Token) fließen als Requirements in die Neuentwicklung ein.
- Keine Löschung von Ledger/Lockbox/Historie; keine Force-Pushes.
